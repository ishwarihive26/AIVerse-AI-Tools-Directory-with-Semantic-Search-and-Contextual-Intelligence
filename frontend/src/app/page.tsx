// This is a placeholder homepage.
// We are NOT building the real UI today - just confirming the app runs.

export default function HomePage() {
  return (
    <main className="p-10">
      <h1 className="text-2xl font-bold">
        AI verse
      </h1>
      <p className="text-sm text-gray-500 mt-2">
        Frontend is connected and running. UI will be built in the next step.
      </p>
    </main>
  );
}
